/**
 * Форматирует числовое значение теста: один знак после запятой, запятая как разделитель.
 * Например: 7 → "7,0", 7.1 → "7,1"
 */
export const formatValue = (value) => Number(value).toFixed(1).replace(".", ",");
